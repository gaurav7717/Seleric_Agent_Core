# Canonical Cube model: mage-ai/infra/cube ONLY.
# Base_Agent/cube/model is a stub — do not mount ./cube here.
#
#   docker compose up -d --force-recreate cube
#   uv run python scripts/smoke_cube.py
#
# Note: if mage-cube is already bound to 127.0.0.1:4001, skip this compose
# and point config.yaml cube.api_url at that instance instead.
name: seleric-mcp

services:
  cube:
    image: cubejs/cube:latest
    container_name: seleric-mcp-cube-1
    restart: unless-stopped
    volumes:
      - ../mage-ai/infra/cube/model:/cube/conf/model:ro
      - ../mage-ai/infra/cube/cube.js:/cube/conf/cube.js:ro
    env_file:
      - ../mage-ai/infra/cube/.env
    ports:
      - "127.0.0.1:${CUBE_HOST_PORT:-4001}:4000"
    extra_hosts:
      - "clickhouse.seleric.com:72.61.228.168"
      - "host.docker.internal:host-gateway"
